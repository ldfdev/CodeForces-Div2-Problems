#include <bits/stdc++.h>

const int sz = 1e5;
int permutation[sz];
int main()
{
    int tests, n, j;
    scanf("%d", &tests);
    while(tests--) {
        scanf("%d", &n);
        for(j = 0; j < n; j++)
            scanf("%d", permutation + j);
            
    }
    return 0;
}