#include <bits/stdc++.h>

typedef long long ll;

int main()
{
    int tests;
    ll n;
    scanf("%d", &tests);
    while(tests--) {
        scanf("%lld", &n);
        printf("%lld\n", n);
    }
    return 0;
}